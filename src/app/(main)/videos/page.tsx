import VideoServer from "@/components/serverComponents/VideoServer";
import React from "react";

export const revalidate = 60;

const Videos = () => {
  return <VideoServer />;
};

export default Videos;
